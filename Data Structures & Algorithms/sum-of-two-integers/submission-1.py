class Solution:
    def getSum(self, a: int, b: int) -> int:
        count = []
        for i in range(a):
            count.append(i)
        for n in range(b):
            count.append(i)
        return len(count)
